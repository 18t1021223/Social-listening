server:
  port: 8001
spring:
  main:
    web-application-type: servlet
  application:
    name: api-service
  jackson:
    serialization:
      write-dates-as-timestamps: true
  datasource:
    url: jdbc:mariadb://localhost:3307/ex
    username: root
    password: 123
    hikari:
      connection-timeout: 1200000
      minimum-idle: 10
      maximum-pool-size: 50
      pool-name: vn.com.sociallistening.manager.api
      driver-class-name: org.mariadb.jdbc.Driver
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
    database: mysql
    show-sql: false
  redis:
    cluster:
      nodes: localhost:7000,localhost:7001,localhost:7002,localhost:7003,localhost:7004,localhost:7005
      max-redirects: 12
  kafka:
    bootstrap-servers: 127.0.0.1:9092
    listener:
      concurrency: 1
    group-id: crawl_project
feign:
  client:
    config:
      default:
        loggerLevel: full
    oauth2:
      url: http://localhost:8000
      client-id: 0ZS4RIXTWcf9a0S9a059Ux42VppRpmLG
      client-secret: rk81o1jobMBiATh153CUY92st6PmB0AW
    storage:
      url: http://localhost:8002